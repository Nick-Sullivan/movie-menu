# The Movie Menu

A website to help prepare meals to serve them at the same
time they appear in a movie.

## Development

Tools are pinned with [mise](https://mise.jdx.dev) (`.mise/config.toml`):

```sh
mise run init
mise run dev
mise run client:test
mise run server:test
```
